{% extends "KamranActivityBundle::layout.html.twig" %}


{%- block title -%}
    Add Site Information
{%- endblock title -%}

{% block body %}

<div class="col-lg-6">
        <fieldset>
            <legend>Example extended forms via view</legend>
                <form class="entity" action="{{ path('tags_create') }}" method="post"  class="form-horizontal">
                    {{ form_errors(form) }}
                    {{ form_rest(form) }}
                    <div class="actions">
                        <button type="submit">Add</button>
                    </div>
                </form>
        </fieldset>
</div>

{% endblock body %}